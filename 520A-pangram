#include <bits/stdc++.h>
using namespace std;
int main(){
    int n; cin >> n;
    string s; cin >> s;
    vector<int> seen(26, 0);
    for(char &c : s) seen[tolower(c)-'a'] = 1;
    cout << (accumulate(seen.begin(), seen.end(), 0) == 26 ? "YES" : "NO");
}

