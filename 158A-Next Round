#include <bits/stdc++.h>
using namespace std;
int main(){
    int n,k; cin>>n>>k;
    vector<int>a(n);
    for(int&i:a)cin>>i;
    int score = a[k-1], cnt=0;
    for(int x:a) if(x>=score && x>0) cnt++;
    cout<<cnt;
}


