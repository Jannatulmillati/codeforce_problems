#include <bits/stdc++.h>
using namespace std;
int main(){
    string s; cin>>s;
    string t;
    for(int i=0;i<s.size();){
        if(s.substr(i,3)=="WUB"){if(!t.empty()&&t.back()!=' ') t+=' '; i+=3;}
        else t+=s[i++];
    }
    if(t.back()==' ') t.pop_back();
    cout<<t;
}



