#include <bits/stdc++.h>
using namespace std;
int main(){
    int n; cin >> n;
    string s, prev = "", curr;
    int groups = 0;
    while(n--){
        cin >> curr;
        if(curr != prev) groups++;
        prev = curr;
    }
    cout << groups;
}

