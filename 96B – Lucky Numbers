

#include <bits/stdc++.h>
using namespace std;
bool lucky(long long x){
    while(x){
        int d=x%10;
        if(d!=4 && d!=7) return false;
        x/=10;
    }
    return true;
}
int main(){
    long long n; cin>>n;
    while(!lucky(n)) n++;
    cout<<n;
}
