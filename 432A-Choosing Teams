#include <bits/stdc++.h>
using namespace std;
int main(){
    int n, k; cin >> n >> k;
    vector<int> a(n);
    for(int &x : a) cin >> x;
    int cnt = 0;
    for(int x : a) if(x + k <= 5) cnt++;
    cout << cnt / 3;
}
